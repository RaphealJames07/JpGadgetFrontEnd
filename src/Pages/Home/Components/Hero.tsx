import HeroCarousel from "./HeroCarousel";

const Hero = () => {
    return (
        <>
            <div className="w-full h-max phone:h-max">
              <HeroCarousel/>
            </div>
        </>
    );
};

export default Hero;
